# Copy to .env and fill only locally.
AWS_PROFILE=fiap-fase3
AWS_REGION=us-east-1
FCG_STACK_NAME=fcg-fase3-notifications
# Fill from SAM outputs after a real deploy.
USER_CREATED_QUEUE_URL=
PAYMENT_PROCESSED_QUEUE_URL=
# Local Kubernetes Secrets will be rendered in stage 8; never commit secrets.
JWT_KEY=
SQL_PASSWORD=
RABBITMQ_USERNAME=
RABBITMQ_PASSWORD=
GRAFANA_ADMIN_PASSWORD=
